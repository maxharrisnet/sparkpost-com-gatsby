import React, { Component } from "react"

const Momentum = ({ pageContext }) => {
  return (
    <div>
      <h1>SparkPost Momentum Article Default Template</h1>
    </div>
  )
}

export default Momentum