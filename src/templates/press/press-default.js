import React, { Component } from "react"

const Press = ({ pageContext }) => {
  const page = pageContext.pageInfo

  return (
    <div>
      <h1>SparkPost Press Default Template</h1>
    </div>
  )
}

export default Press